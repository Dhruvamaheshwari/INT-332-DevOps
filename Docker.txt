// manually run and delete the container
    docker run -it ubuntu
    docker rm <container id>

// automaticall delete when you exit the ubuntu
    docker run --rm -it ubuntu

// env file command 
    docker run --env-file .env node

// if you want to print the env variable
    docker run --env-file .env node env

// port mapping
    docker rum -p 3000:80 nginx {3000 is system port and 80 is nginx port jispe nginx run ho rha h to hum mapping kra rhe 3000 pe}


why use use rmi -f => forcefully delete the image {rmi only delte the stop container but if image or container is runnign so we can use the " rmi -f "}

